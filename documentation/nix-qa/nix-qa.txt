home-manager.inputs.nixpkgs.follows = "nixpkgs";
# Prevent Home Manager from downloading its own version of nixpkgs.

The ... in outputs = { self, nixpkgs, home-manager, ... }:
# By adding the ..., you are saying: "I only care about using self, nixpkgs,
# and home-manager right now, but feel free to pass in whatever else you
# have; I'll just ignore the rest."

# Open and close
Super + Q
Super + C
Super + V # Floating Mode
Super + R: Open an app launcher (if you have wofi or rofi installed). Not Yet


# reboot
uwsm stop && reboot
# shutdown
uwsm stop && shutdown now
# logout
uwsm stop
# Sleep
systemctl suspend


sudo nixos-rebuild switch 及时没有 --flake也可以
